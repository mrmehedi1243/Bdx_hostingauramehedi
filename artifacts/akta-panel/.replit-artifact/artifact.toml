kind = "web"
previewPath = "/"
title = "Akta Hosting Panel"
version = "1.0.0"
id = "artifacts/akta-panel"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24041

[services.development]
run = "pnpm --filter @workspace/akta-panel run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/akta-panel", "run", "build" ]
publicDir = "artifacts/akta-panel/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24041"
BASE_PATH = "/"
